import { useMemo, useState } from 'react';
import { QueryClient, QueryClientProvider, useQueryClient } from '@tanstack/react-query';
import { Link, Route, Switch, useLocation } from 'wouter';
import {
  AlertTriangle, ArrowDownRight, ArrowUpRight, BarChart3, Banknote, Bell, Boxes, Building2,
  CalendarDays, Check, ChevronRight, CircleDollarSign, ClipboardList, CreditCard, FileDown,
  FileText, HandCoins, LayoutDashboard, Menu, PackagePlus, Pencil, Plus, Printer, Receipt,
  RefreshCw, Search, Settings2, ShoppingCart, Store, Truck, UserRound, Users, Wallet, X,
} from 'lucide-react';
import {
  getGetBuyersQueryKey, getGetDashboardQueryKey, getGetExpensesQueryKey, getGetLoansQueryKey,
  getGetProductsQueryKey, getGetSalesQueryKey, getGetSuppliersQueryKey, getHealthCheckQueryKey,
  useCreateBuyer, useCreateExpense, useCreateLoan, useCreateProduct, useCreateSale, useCreateSupplier,
  useGetBuyers, useGetDashboard, useGetExpenses, useGetLoans, useGetProducts, useGetSales,
  useGetSuppliers, useHealthCheck, useUpdateBuyer,
} from '@workspace/api-client-react';
import { ErrorBoundary } from '@/components/error-boundary';
import { Toaster } from '@/components/ui/toaster';
import { TooltipProvider } from '@/components/ui/tooltip';

const queryClient = new QueryClient();
const money = (value: number | undefined) => `Rs ${Math.round(value || 0).toLocaleString('en-PK')}`;
const shortDate = (value?: string) => value ? new Date(value).toLocaleDateString('en-PK', { day: '2-digit', month: 'short' }) : '—';
const timeDate = (value?: string) => value ? new Date(value).toLocaleTimeString('en-PK', { hour: '2-digit', minute: '2-digit' }) : '—';

function Button({ children, onClick, variant = 'primary', type = 'button', className = '', disabled = false, testId }: { children: React.ReactNode; onClick?: () => void; variant?: 'primary' | 'outline' | 'ghost' | 'danger'; type?: 'button' | 'submit'; className?: string; disabled?: boolean; testId: string }) {
  const styles = { primary: 'bg-primary text-primary-foreground hover:brightness-110', outline: 'border border-border bg-card hover:bg-muted', ghost: 'hover:bg-muted text-muted-foreground', danger: 'border border-destructive/30 text-destructive hover:bg-destructive/10' };
  return <button data-testid={testId} disabled={disabled} type={type} onClick={onClick} className={`inline-flex items-center justify-center gap-2 rounded-lg px-3.5 py-2 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50 ${styles[variant]} ${className}`}>{children}</button>;
}

function Field({ label, name, value, onChange, type = 'text', required = false, placeholder = '' }: { label: string; name: string; value: string | number; onChange: (v: string) => void; type?: string; required?: boolean; placeholder?: string }) {
  return <label className="grid gap-1.5 text-xs font-semibold text-muted-foreground">{label}<input data-testid={`input-${name}`} required={required} name={name} type={type} value={value} onChange={e => onChange(e.target.value)} placeholder={placeholder} className="h-10 rounded-lg border border-input bg-background px-3 text-sm font-medium text-foreground outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/15" /></label>;
}

function Modal({ title, eyebrow, children, onClose }: { title: string; eyebrow?: string; children: React.ReactNode; onClose: () => void }) {
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-foreground/30 p-0 backdrop-blur-sm sm:items-center sm:p-6" onMouseDown={e => e.target === e.currentTarget && onClose()}>
    <div className="animate-in w-full max-w-lg rounded-t-2xl border border-border bg-card p-6 shadow-2xl sm:rounded-2xl">
      <div className="mb-5 flex items-start justify-between"><div><div className="font-mono text-[10px] font-bold uppercase tracking-[.18em] text-accent">{eyebrow || 'New record'}</div><h2 className="mt-1 text-xl font-bold tracking-tight">{title}</h2></div><button data-testid="button-close-modal" onClick={onClose} className="rounded-lg p-2 text-muted-foreground hover:bg-muted"><X size={18} /></button></div>
      {children}
    </div>
  </div>;
}

function Empty({ title, detail, action }: { title: string; detail: string; action?: React.ReactNode }) {
  return <div className="flex min-h-44 flex-col items-center justify-center rounded-xl border border-dashed border-border bg-background/50 p-6 text-center"><div className="mb-3 grid h-10 w-10 place-items-center rounded-full bg-secondary/30 text-primary"><ClipboardList size={18} /></div><h3 className="font-bold">{title}</h3><p className="mt-1 max-w-xs text-sm text-muted-foreground">{detail}</p>{action && <div className="mt-4">{action}</div>}</div>;
}

function Loading({ rows = 4 }: { rows?: number }) {
  return <div className="grid gap-3">{Array.from({ length: rows }).map((_, i) => <div key={i} className="h-12 animate-pulse rounded-lg bg-muted/70" />)}</div>;
}
function Failed({ onRetry }: { onRetry: () => void }) {
  return <div className="flex items-center justify-between rounded-xl border border-destructive/25 bg-destructive/5 p-4 text-sm"><div><strong className="text-destructive">Could not load this view.</strong><div className="text-muted-foreground">Check the connection and try again.</div></div><Button variant="outline" onClick={onRetry} testId="button-retry"><RefreshCw size={15} /> Retry</Button></div>;
}

const nav = [
  { href: '/', label: 'Overview', icon: LayoutDashboard },
  { href: '/sales', label: 'Sales desk', icon: ShoppingCart },
  { href: '/buyers', label: 'Buyers', icon: Users },
  { href: '/products', label: 'Products', icon: Boxes },
  { href: '/suppliers', label: 'Suppliers', icon: Truck },
  { href: '/expenses', label: 'Expenses', icon: Receipt },
  { href: '/loans', label: 'Loans', icon: HandCoins },
  { href: '/reports', label: 'Reports', icon: BarChart3 },
];

function Shell({ children }: { children: React.ReactNode }) {
  const [location, setLocation] = useLocation();
  const [open, setOpen] = useState(false);
  const health = useHealthCheck({ query: { queryKey: getHealthCheckQueryKey(), refetchInterval: 30000 } });
  const active = nav.find(item => item.href === location)?.label || 'Overview';
  return <div className="min-h-[100dvh] bg-background">
    <aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r border-sidebar-border bg-sidebar text-sidebar-foreground transition-transform duration-300 md:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}>
      <div className="flex h-full flex-col p-4">
        <div className="flex items-center gap-3 px-2 py-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-secondary text-primary"><Store size={21} /></div><div><div className="text-[15px] font-bold tracking-tight">Zubair Traders</div><div className="font-mono text-[9px] uppercase tracking-[.18em] text-sidebar-foreground/55">Bakery operations</div></div></div>
        <div className="my-6 h-px bg-sidebar-border" />
        <div className="mb-2 px-3 font-mono text-[9px] font-bold uppercase tracking-[.2em] text-sidebar-foreground/40">Workspace</div>
        <nav className="grid gap-1">{nav.map(item => { const Icon = item.icon; const isActive = item.href === location; return <Link key={item.href} href={item.href} data-testid={`link-nav-${item.label.toLowerCase().replace(' ', '-')}`} onClick={() => setOpen(false)} className={`group flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-semibold transition ${isActive ? 'bg-sidebar-accent text-secondary' : 'text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground'}`}><Icon size={17} strokeWidth={isActive ? 2.4 : 1.8} /><span>{item.label}</span>{isActive && <ChevronRight size={14} className="ml-auto" />}</Link>; })}</nav>
        <div className="mt-auto rounded-xl border border-sidebar-border bg-sidebar-accent/60 p-3"><div className="flex items-center gap-2 text-xs font-semibold"><span className={`h-2 w-2 rounded-full ${health.isError ? 'bg-destructive' : 'bg-emerald-400'}`} />System {health.isError ? 'offline' : 'connected'}</div><p className="mt-1 text-[11px] leading-relaxed text-sidebar-foreground/50">Ready for the morning run.</p></div>
      </div>
    </aside>
    {open && <button aria-label="Close menu" data-testid="button-close-menu" className="fixed inset-0 z-30 bg-foreground/25 md:hidden" onClick={() => setOpen(false)} />}
    <div className="md:pl-64"><header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b border-border/80 bg-background/90 px-4 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button data-testid="button-open-menu" className="rounded-lg p-2 hover:bg-muted md:hidden" onClick={() => setOpen(true)}><Menu size={19} /></button><div><div className="text-[11px] font-semibold text-muted-foreground">Monday, 18 March 2024</div><h1 className="text-lg font-bold tracking-tight">{active}</h1></div></div><div className="flex items-center gap-2"><button data-testid="button-notifications" className="relative rounded-lg p-2 text-muted-foreground hover:bg-muted"><Bell size={18} /><span className="absolute right-1.5 top-1.5 h-1.5 w-1.5 rounded-full bg-accent" /></button><div className="hidden h-7 w-px bg-border sm:block" /><div className="flex items-center gap-2 pl-1"><div className="grid h-8 w-8 place-items-center rounded-full bg-primary text-xs font-bold text-primary-foreground">ZA</div><span className="hidden text-sm font-semibold sm:block">Zubair Ahmed</span><Settings2 className="ml-1 hidden text-muted-foreground sm:block" size={15} /></div></div></header><main className="mx-auto max-w-[1500px] p-4 md:p-8">{children}</main></div>
  </div>;
}

function PageIntro({ eyebrow, title, detail, action }: { eyebrow: string; title: string; detail: string; action?: React.ReactNode }) {
  return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="font-mono text-[10px] font-bold uppercase tracking-[.22em] text-accent">{eyebrow}</div><h2 className="mt-1 text-3xl font-bold tracking-[-.04em] text-foreground md:text-4xl">{title}</h2><p className="mt-2 max-w-2xl text-sm text-muted-foreground">{detail}</p></div>{action}</div>;
}
function Stat({ label, value, note, icon: Icon, tone = 'primary' }: { label: string; value: string; note: string; icon: typeof Banknote; tone?: 'primary' | 'accent' | 'warning' | 'blue' }) {
  const colors = { primary: 'bg-primary text-primary-foreground', accent: 'bg-accent text-accent-foreground', warning: 'bg-secondary text-primary', blue: 'bg-[#dce9e7] text-primary' };
  return <div className="panel shine animate-in rounded-xl p-4"><div className="flex items-start justify-between"><div className="text-[11px] font-bold uppercase tracking-[.1em] text-muted-foreground">{label}</div><div className={`grid h-8 w-8 place-items-center rounded-lg ${colors[tone]}`}><Icon size={16} /></div></div><div data-testid={`text-stat-${label.toLowerCase().replaceAll(' ', '-')}`} className="mt-4 font-mono text-2xl font-bold tracking-tight">{value}</div><div className="mt-1 text-xs text-muted-foreground">{note}</div></div>;
}

function Dashboard() {
  const q = useGetDashboard({ query: { queryKey: getGetDashboardQueryKey() } });
  if (q.isLoading) return <><PageIntro eyebrow="Good morning" title="The floor at a glance" detail="Loading today's rhythm…" /><Loading rows={6} /></>;
  if (q.isError) return <><PageIntro eyebrow="Good morning" title="The floor at a glance" detail="Your command view for the day." /><Failed onRetry={() => q.refetch()} /></>;
  const d = q.data;
  const trend = d?.salesTrend || [];
  const max = Math.max(...trend.map(p => p.value), 1);
  return <div className="animate-in">
    <PageIntro eyebrow="Monday · morning shift" title="The floor at a glance" detail="Everything you need to move from sale to cash collection in seconds." action={<Link href="/sales" data-testid="link-start-sale" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground shadow-sm transition hover:brightness-110"><Plus size={16} /> Start a sale</Link>} />
    <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">
      <Stat label="Today's sales" value={money(d?.dailySales)} note="Across all invoices" icon={Banknote} />
      <Stat label="Today's profit" value={money(d?.todaysProfit)} note="After product cost" icon={ArrowUpRight} tone="warning" />
      <Stat label="Buyer debt" value={money(d?.buyerDebt)} note="Collect when you can" icon={CreditCard} tone="accent" />
      <Stat label="Supplier owed" value={money(d?.supplierOwed)} note="Purchase-facing balance" icon={Truck} tone="blue" />
      <Stat label="Cash in drawer" value={money(d?.cashInDrawer)} note="Expected on hand" icon={Wallet} tone="primary" />
    </div>
    <div className="mt-5 grid gap-5 xl:grid-cols-[1.4fr_.8fr]">
      <section className="panel rounded-xl p-5"><div className="flex items-center justify-between"><div><h3 className="font-bold">Sales rhythm</h3><p className="mt-1 text-xs text-muted-foreground">Last 7 trading days</p></div><div className="rounded-md bg-secondary/30 px-2 py-1 font-mono text-[10px] font-bold text-primary">GROSS SALES</div></div><div className="mt-7 flex h-52 items-end gap-2 sm:gap-5">{trend.length ? trend.map(point => <div key={point.label} className="group flex min-w-0 flex-1 flex-col items-center gap-2"><div className="relative flex h-40 w-full items-end justify-center"><div className="absolute -top-6 rounded bg-primary px-1.5 py-1 font-mono text-[9px] text-primary-foreground opacity-0 transition group-hover:opacity-100">{money(point.value)}</div><div className="w-full max-w-10 rounded-t-md bg-primary/80 transition-all duration-500 group-hover:bg-accent" style={{ height: `${Math.max((point.value / max) * 100, 5)}%` }} /></div><span className="font-mono text-[10px] text-muted-foreground">{point.label}</span></div>) : <div className="w-full"><Empty title="No sales rhythm yet" detail="Your first invoice will give this chart a pulse." /></div>}</div></section>
      <section className="panel rounded-xl p-5"><div className="flex items-start justify-between"><div><h3 className="font-bold">Low stock watch</h3><p className="mt-1 text-xs text-muted-foreground">Restock before the next bake</p></div><Link href="/products" data-testid="link-view-stock" className="text-xs font-bold text-primary hover:underline">View stock</Link></div><div className="mt-5 grid gap-1">{d?.lowStock?.length ? d.lowStock.map(p => <div key={p.id} data-testid={`row-low-stock-${p.id}`} className="flex items-center justify-between rounded-lg px-3 py-3 transition hover:bg-muted"><div><div className="text-sm font-bold">{p.name}</div><div className="text-xs text-muted-foreground">Min {p.minStockAlert} {p.unit}</div></div><div className="text-right"><div className="font-mono text-sm font-bold text-destructive">{p.stockQuantity} {p.unit}</div><div className="text-[10px] text-muted-foreground">remaining</div></div></div>) : <Empty title="Stock is comfortable" detail="Nothing needs your attention right now." />}</div></section>
    </div>
    <section className="panel mt-5 rounded-xl p-5"><div className="flex items-center justify-between"><div><h3 className="font-bold">Recent activity</h3><p className="mt-1 text-xs text-muted-foreground">What moved through the business</p></div><FileText size={18} className="text-muted-foreground" /></div><div className="mt-4 grid divide-y divide-border/70">{d?.recentActivity?.length ? d.recentActivity.map(a => <div key={a.id} data-testid={`row-activity-${a.id}`} className="flex items-center gap-3 py-3"><div className={`grid h-8 w-8 shrink-0 place-items-center rounded-lg ${a.type === 'payment' ? 'bg-secondary text-primary' : 'bg-muted text-muted-foreground'}`}>{a.type === 'payment' ? <CircleDollarSign size={16} /> : <Receipt size={16} />}</div><div className="min-w-0 flex-1"><div className="truncate text-sm font-semibold">{a.title}</div><div className="truncate text-xs text-muted-foreground">{a.detail}</div></div><div className="shrink-0 font-mono text-[10px] text-muted-foreground">{a.time}</div></div>) : <Empty title="The day is just beginning" detail="Sales, purchases and collections will appear here." />}</div></section>
  </div>;
}

function Sales() {
  const sales = useGetSales({ query: { queryKey: getGetSalesQueryKey() } });
  const buyers = useGetBuyers({ query: { queryKey: getGetBuyersQueryKey() } });
  const create = useCreateSale(); const qc = useQueryClient(); const [done, setDone] = useState(false);
  const [form, setForm] = useState({ buyerId: '', totalAmount: '', paidAmount: '', notes: '' });
  const total = Number(form.totalAmount) || 0; const paid = Number(form.paidAmount) || 0; const due = Math.max(total - paid, 0);
  const submit = (e: React.FormEvent) => { e.preventDefault(); create.mutate({ data: { buyerId: Number(form.buyerId), totalAmount: total, paidAmount: paid, paymentStatus: due ? 'partial' : 'paid', notes: form.notes || undefined } }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetSalesQueryKey() }); qc.invalidateQueries({ queryKey: getGetDashboardQueryKey() }); setDone(true); setForm({ buyerId: '', totalAmount: '', paidAmount: '', notes: '' }); } }); };
  return <div className="animate-in"><PageIntro eyebrow="Fast lane" title="Make a sale" detail="A clean invoice now means a clean drawer later." /><div className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]">
    <section className="panel rounded-xl p-5"><div className="mb-5 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-secondary text-primary"><ShoppingCart size={18} /></div><div><h3 className="font-bold">New invoice</h3><p className="text-xs text-muted-foreground">Press through the essentials</p></div></div><form onSubmit={submit} className="grid gap-4"><label className="grid gap-1.5 text-xs font-semibold text-muted-foreground">Buyer<select data-testid="select-buyer" required value={form.buyerId} onChange={e => setForm({ ...form, buyerId: e.target.value })} className="h-10 rounded-lg border border-input bg-background px-3 text-sm font-medium text-foreground outline-none focus:border-primary"><option value="">Choose a buyer…</option>{buyers.data?.map(b => <option key={b.id} value={b.id}>{b.name} · {b.phone}</option>)}</select></label><div className="grid grid-cols-2 gap-3"><Field label="Sale total" name="total-amount" type="number" value={form.totalAmount} onChange={v => setForm({ ...form, totalAmount: v })} required placeholder="0" /><Field label="Paid now" name="paid-amount" type="number" value={form.paidAmount} onChange={v => setForm({ ...form, paidAmount: v })} required placeholder="0" /></div><Field label="Note (optional)" name="sale-note" value={form.notes} onChange={v => setForm({ ...form, notes: v })} placeholder="Delivery route, order detail…" /><div className="rounded-lg bg-muted p-3"><div className="flex justify-between text-xs text-muted-foreground"><span>Due on this invoice</span><span className="font-mono font-bold text-foreground">{money(due)}</span></div></div><Button type="submit" disabled={create.isPending || !form.buyerId} testId="button-create-sale" className="h-11">{create.isPending ? 'Saving invoice…' : <><Check size={17} /> Confirm sale</>}</Button>{done && <div data-testid="status-sale-success" className="flex items-center gap-2 text-xs font-semibold text-emerald-700"><Check size={14} /> Invoice posted. Ready for the next one.</div>}</form></section>
    <section className="panel rounded-xl p-5"><div className="flex items-center justify-between"><div><h3 className="font-bold">Recent invoices</h3><p className="mt-1 text-xs text-muted-foreground">Print or share after every sale</p></div><Button variant="outline" onClick={() => window.print()} testId="button-print-sales"><Printer size={15} /> Print list</Button></div><div className="mt-5 overflow-x-auto">{sales.isLoading ? <Loading /> : sales.isError ? <Failed onRetry={() => sales.refetch()} /> : sales.data?.length ? <table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Invoice</th><th className="pb-3">Buyer</th><th className="pb-3">Total</th><th className="pb-3">Status</th><th className="pb-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border/70">{sales.data.map(s => <tr key={s.id} data-testid={`row-sale-${s.id}`}><td className="py-3 font-mono text-xs font-bold">{s.invoiceNumber}</td><td className="py-3 font-semibold">{s.buyerName}<div className="text-[10px] text-muted-foreground">{timeDate(s.transactionTime)}</div></td><td className="py-3 font-mono">{money(s.totalAmount)}<div className="text-[10px] text-muted-foreground">Due {money(s.dueAmount)}</div></td><td className="py-3"><span data-testid={`status-sale-${s.id}`} className={`rounded-full px-2 py-1 text-[10px] font-bold uppercase ${s.paymentStatus === 'paid' ? 'bg-emerald-100 text-emerald-800' : 'bg-secondary text-primary'}`}>{s.paymentStatus}</span></td><td className="py-3 text-right"><div className="flex justify-end gap-1"><button data-testid={`button-pdf-${s.id}`} title="Print invoice" onClick={() => window.print()} className="rounded-md p-2 text-muted-foreground hover:bg-muted"><FileDown size={15} /></button><button data-testid={`button-whatsapp-${s.id}`} title="Share invoice" onClick={() => navigator.share?.({ title: s.invoiceNumber, text: `${s.invoiceNumber} · ${s.buyerName} · ${money(s.totalAmount)}` })} className="rounded-md p-2 text-muted-foreground hover:bg-muted"><HandCoins size={15} /></button></div></td></tr>)}</tbody></table> : <Empty title="No invoices yet" detail="Your next sale will appear here for fast follow-up." />}</div></section>
  </div></div>;
}

function Buyers() {
  const q = useGetBuyers({ query: { queryKey: getGetBuyersQueryKey() } }); const create = useCreateBuyer(); const update = useUpdateBuyer(); const qc = useQueryClient();
  const [search, setSearch] = useState(''); const [editing, setEditing] = useState<any>(null); const [modal, setModal] = useState(false);
  const blank = { name: '', phone: '', cnic: '', address: '', creditLimit: '' }; const [form, setForm] = useState(blank);
  const list = useMemo(() => (q.data || []).filter(b => `${b.name} ${b.phone}`.toLowerCase().includes(search.toLowerCase())), [q.data, search]);
  const open = (b?: any) => { setEditing(b || null); setForm(b ? { name: b.name, phone: b.phone, cnic: b.cnic, address: b.address, creditLimit: String(b.creditLimit) } : blank); setModal(true); };
  const submit = (e: React.FormEvent) => { e.preventDefault(); const data = { ...form, creditLimit: Number(form.creditLimit) }; const finish = () => { qc.invalidateQueries({ queryKey: getGetBuyersQueryKey() }); setModal(false); }; editing ? update.mutate({ id: editing.id, data }, { onSuccess: finish }) : create.mutate({ data }, { onSuccess: finish }); };
  return <div className="animate-in"><PageIntro eyebrow="People who keep the ovens moving" title="Buyers" detail="Know who owes, what they buy, and where to reach them." action={<Button onClick={() => open()} testId="button-add-buyer"><Plus size={16} /> Add buyer</Button>} /><div className="panel rounded-xl p-5"><div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="relative max-w-sm flex-1"><Search size={16} className="absolute left-3 top-3 text-muted-foreground" /><input data-testid="input-search-buyers" value={search} onChange={e => setSearch(e.target.value)} placeholder="Search name or phone…" className="h-10 w-full rounded-lg border border-input bg-background pl-9 pr-3 text-sm outline-none focus:border-primary" /></div><div className="font-mono text-xs text-muted-foreground">{list.length} buyers</div></div>{q.isLoading ? <Loading /> : q.isError ? <Failed onRetry={() => q.refetch()} /> : list.length ? <div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Buyer</th><th className="pb-3">Phone</th><th className="pb-3">Credit limit</th><th className="pb-3">Current udhaar</th><th className="pb-3 text-right">Edit</th></tr></thead><tbody className="divide-y divide-border/70">{list.map(b => <tr key={b.id} data-testid={`row-buyer-${b.id}`}><td className="py-3"><div className="font-semibold">{b.name}</div><div className="text-[11px] text-muted-foreground">{b.address || 'No address'}</div></td><td className="py-3 font-mono text-xs">{b.phone}</td><td className="py-3 font-mono">{money(b.creditLimit)}</td><td className="py-3"><span className={`font-mono font-bold ${b.currentBalance > 0 ? 'text-accent' : 'text-emerald-700'}`}>{money(b.currentBalance)}</span></td><td className="py-3 text-right"><button data-testid={`button-edit-buyer-${b.id}`} onClick={() => open(b)} className="rounded-md p-2 text-muted-foreground hover:bg-muted"><Pencil size={15} /></button></td></tr>)}</tbody></table></div> : <Empty title={search ? 'No buyers match that search' : 'Your buyer book is empty'} detail="Add your first wholesale buyer to start tracking udhaar." action={!search && <Button onClick={() => open()} testId="button-empty-add-buyer"><Plus size={15} /> Add buyer</Button>} />}</div>{modal && <Modal title={editing ? 'Edit buyer' : 'Add buyer'} eyebrow="Buyer book" onClose={() => setModal(false)}><form onSubmit={submit} className="grid gap-3"><Field label="Name" name="buyer-name" value={form.name} onChange={v => setForm({ ...form, name: v })} required /><div className="grid grid-cols-2 gap-3"><Field label="Phone" name="buyer-phone" value={form.phone} onChange={v => setForm({ ...form, phone: v })} required /><Field label="CNIC" name="buyer-cnic" value={form.cnic} onChange={v => setForm({ ...form, cnic: v })} /></div><Field label="Address" name="buyer-address" value={form.address} onChange={v => setForm({ ...form, address: v })} /><Field label="Credit limit" name="buyer-limit" type="number" value={form.creditLimit} onChange={v => setForm({ ...form, creditLimit: v })} required /><Button type="submit" disabled={create.isPending || update.isPending} testId="button-save-buyer">{create.isPending || update.isPending ? 'Saving…' : 'Save buyer'}</Button></form></Modal>}</div>;
}

function Suppliers() {
  const q = useGetSuppliers({ query: { queryKey: getGetSuppliersQueryKey() } }); const create = useCreateSupplier(); const qc = useQueryClient(); const [modal, setModal] = useState(false); const [form, setForm] = useState({ name: '', phone: '', companyName: '', address: '' });
  const submit = (e: React.FormEvent) => { e.preventDefault(); create.mutate({ data: form }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetSuppliersQueryKey() }); setModal(false); setForm({ name: '', phone: '', companyName: '', address: '' }); } }); };
  return <div className="animate-in"><PageIntro eyebrow="Keep the chain steady" title="Suppliers" detail="Your purchase-facing balances, in one quiet place." action={<Button onClick={() => setModal(true)} testId="button-add-supplier"><Plus size={16} /> Add supplier</Button>} /><div className="panel overflow-x-auto rounded-xl p-5">{q.isLoading ? <Loading /> : q.isError ? <Failed onRetry={() => q.refetch()} /> : q.data?.length ? <table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Supplier</th><th className="pb-3">Contact</th><th className="pb-3">Company</th><th className="pb-3">Owed</th></tr></thead><tbody className="divide-y divide-border/70">{q.data.map(s => <tr key={s.id} data-testid={`row-supplier-${s.id}`}><td className="py-4 font-semibold">{s.name}</td><td className="py-4 font-mono text-xs">{s.phone}</td><td className="py-4 text-muted-foreground">{s.companyName || 'Independent'}</td><td className="py-4 font-mono font-bold text-accent">{money(s.currentBalance)}</td></tr>)}</tbody></table> : <Empty title="No suppliers yet" detail="Build your purchase network as you go." action={<Button onClick={() => setModal(true)} testId="button-empty-add-supplier"><Plus size={15} /> Add supplier</Button>} />}</div>{modal && <Modal title="Add supplier" eyebrow="Supply book" onClose={() => setModal(false)}><form onSubmit={submit} className="grid gap-3"><Field label="Name" name="supplier-name" value={form.name} onChange={v => setForm({ ...form, name: v })} required /><Field label="Phone" name="supplier-phone" value={form.phone} onChange={v => setForm({ ...form, phone: v })} required /><Field label="Company" name="supplier-company" value={form.companyName} onChange={v => setForm({ ...form, companyName: v })} /><Field label="Address" name="supplier-address" value={form.address} onChange={v => setForm({ ...form, address: v })} /><Button type="submit" disabled={create.isPending} testId="button-save-supplier">{create.isPending ? 'Saving…' : 'Save supplier'}</Button></form></Modal>}</div>;
}

function Products() {
  const q = useGetProducts({ query: { queryKey: getGetProductsQueryKey() } }); const create = useCreateProduct(); const qc = useQueryClient(); const [modal, setModal] = useState(false); const [form, setForm] = useState({ name: '', unit: 'pcs', purchaseCost: '', sellingPrice: '', stockQuantity: '', minStockAlert: '' });
  const submit = (e: React.FormEvent) => { e.preventDefault(); create.mutate({ data: { ...form, purchaseCost: Number(form.purchaseCost), sellingPrice: Number(form.sellingPrice), stockQuantity: Number(form.stockQuantity), minStockAlert: Number(form.minStockAlert) } }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetProductsQueryKey() }); setModal(false); } }); };
  return <div className="animate-in"><PageIntro eyebrow="The bake, counted" title="Products" detail="Inventory with enough signal to keep production moving." action={<Button onClick={() => setModal(true)} testId="button-add-product"><PackagePlus size={16} /> Add product</Button>} /><div className="grid gap-4 sm:grid-cols-3"><div className="panel rounded-xl p-4"><div className="text-xs font-bold uppercase text-muted-foreground">Catalog items</div><div className="mt-2 font-mono text-2xl font-bold">{q.data?.length || 0}</div></div><div className="panel rounded-xl p-4"><div className="text-xs font-bold uppercase text-muted-foreground">Needs restock</div><div className="mt-2 font-mono text-2xl font-bold text-accent">{q.data?.filter(p => p.stockQuantity <= p.minStockAlert).length || 0}</div></div><div className="panel rounded-xl p-4"><div className="text-xs font-bold uppercase text-muted-foreground">Stock value</div><div className="mt-2 font-mono text-2xl font-bold">{money(q.data?.reduce((a, p) => a + p.stockQuantity * p.purchaseCost, 0))}</div></div></div><div className="panel mt-5 overflow-x-auto rounded-xl p-5">{q.isLoading ? <Loading /> : q.isError ? <Failed onRetry={() => q.refetch()} /> : q.data?.length ? <table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Product</th><th className="pb-3">Unit</th><th className="pb-3">Cost</th><th className="pb-3">Sell</th><th className="pb-3">Stock</th><th className="pb-3">Signal</th></tr></thead><tbody className="divide-y divide-border/70">{q.data.map(p => { const low = p.stockQuantity <= p.minStockAlert; return <tr key={p.id} data-testid={`row-product-${p.id}`}><td className="py-3 font-semibold">{p.name}</td><td className="py-3 text-muted-foreground">{p.unit}</td><td className="py-3 font-mono">{money(p.purchaseCost)}</td><td className="py-3 font-mono font-bold">{money(p.sellingPrice)}</td><td className="py-3 font-mono">{p.stockQuantity}</td><td className="py-3">{low ? <span className="inline-flex items-center gap-1 text-xs font-bold text-accent"><AlertTriangle size={14} /> Restock</span> : <span className="text-xs font-semibold text-emerald-700">Healthy</span>}</td></tr>; })}</tbody></table> : <Empty title="No products in the catalog" detail="Add the things that come out of your ovens and off your shelves." action={<Button onClick={() => setModal(true)} testId="button-empty-add-product"><Plus size={15} /> Add product</Button>} />}</div>{modal && <Modal title="Add product" eyebrow="Product catalog" onClose={() => setModal(false)}><form onSubmit={submit} className="grid gap-3"><Field label="Product name" name="product-name" value={form.name} onChange={v => setForm({ ...form, name: v })} required /><div className="grid grid-cols-2 gap-3"><Field label="Unit" name="product-unit" value={form.unit} onChange={v => setForm({ ...form, unit: v })} /><Field label="Opening stock" name="product-stock" type="number" value={form.stockQuantity} onChange={v => setForm({ ...form, stockQuantity: v })} /></div><div className="grid grid-cols-2 gap-3"><Field label="Purchase cost" name="product-cost" type="number" value={form.purchaseCost} onChange={v => setForm({ ...form, purchaseCost: v })} required /><Field label="Selling price" name="product-price" type="number" value={form.sellingPrice} onChange={v => setForm({ ...form, sellingPrice: v })} required /></div><Field label="Low stock alert at" name="product-alert" type="number" value={form.minStockAlert} onChange={v => setForm({ ...form, minStockAlert: v })} required /><Button type="submit" disabled={create.isPending} testId="button-save-product">{create.isPending ? 'Saving…' : 'Save product'}</Button></form></Modal>}</div>;
}

function Expenses() {
  const q = useGetExpenses({ query: { queryKey: getGetExpensesQueryKey() } }); const create = useCreateExpense(); const qc = useQueryClient(); const [modal, setModal] = useState(false); const [form, setForm] = useState({ category: 'Utilities', amount: '', description: '', expenseDate: new Date().toISOString().slice(0, 10) });
  const total = q.data?.reduce((a, e) => a + e.amount, 0) || 0; const submit = (e: React.FormEvent) => { e.preventDefault(); create.mutate({ data: { ...form, amount: Number(form.amount) } }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetExpensesQueryKey() }); setModal(false); } }); };
  return <div className="animate-in"><PageIntro eyebrow="Know the daily burn" title="Expenses" detail="Keep small costs visible before they become a surprise." action={<Button onClick={() => setModal(true)} testId="button-add-expense"><Plus size={16} /> Add expense</Button>} /><div className="mb-5 panel flex items-center justify-between rounded-xl p-5"><div><div className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Logged expenses</div><div className="mt-1 font-mono text-2xl font-bold">{money(total)}</div></div><div className="grid h-10 w-10 place-items-center rounded-lg bg-accent/15 text-accent"><Receipt size={19} /></div></div><div className="panel overflow-x-auto rounded-xl p-5">{q.isLoading ? <Loading /> : q.isError ? <Failed onRetry={() => q.refetch()} /> : q.data?.length ? <table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Date</th><th className="pb-3">Category</th><th className="pb-3">Description</th><th className="pb-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-border/70">{q.data.map(e => <tr key={e.id} data-testid={`row-expense-${e.id}`}><td className="py-3 font-mono text-xs">{shortDate(e.expenseDate)}</td><td className="py-3"><span className="rounded bg-muted px-2 py-1 text-xs font-semibold">{e.category}</span></td><td className="py-3 text-muted-foreground">{e.description || '—'}</td><td className="py-3 text-right font-mono font-bold">{money(e.amount)}</td></tr>)}</tbody></table> : <Empty title="No expenses logged" detail="Add fuel, utilities and daily running costs here." action={<Button onClick={() => setModal(true)} testId="button-empty-add-expense"><Plus size={15} /> Add expense</Button>} />}</div>{modal && <Modal title="Add expense" eyebrow="Daily costs" onClose={() => setModal(false)}><form onSubmit={submit} className="grid gap-3"><Field label="Category" name="expense-category" value={form.category} onChange={v => setForm({ ...form, category: v })} required /><Field label="Amount" name="expense-amount" type="number" value={form.amount} onChange={v => setForm({ ...form, amount: v })} required /><Field label="Description" name="expense-description" value={form.description} onChange={v => setForm({ ...form, description: v })} /><Field label="Date" name="expense-date" type="date" value={form.expenseDate} onChange={v => setForm({ ...form, expenseDate: v })} /><Button type="submit" disabled={create.isPending} testId="button-save-expense">{create.isPending ? 'Saving…' : 'Save expense'}</Button></form></Modal>}</div>;
}

function Loans() {
  const q = useGetLoans({ query: { queryKey: getGetLoansQueryKey() } }); const create = useCreateLoan(); const qc = useQueryClient(); const [modal, setModal] = useState(false); const [form, setForm] = useState({ personName: '', phone: '', loanType: 'lent', amount: '', description: '' });
  const outstanding = q.data?.reduce((a, l) => a + l.balanceRemaining, 0) || 0; const submit = (e: React.FormEvent) => { e.preventDefault(); create.mutate({ data: { ...form, amount: Number(form.amount) } }, { onSuccess: () => { qc.invalidateQueries({ queryKey: getGetLoansQueryKey() }); setModal(false); } }); };
  return <div className="animate-in"><PageIntro eyebrow="Money outside the drawer" title="Loans" detail="Borrowed or lent, every rupee gets a clear line back." action={<Button onClick={() => setModal(true)} testId="button-add-loan"><Plus size={16} /> Add loan</Button>} /><div className="mb-5 grid gap-4 sm:grid-cols-2"><div className="panel rounded-xl p-5"><div className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Outstanding balance</div><div className="mt-2 font-mono text-2xl font-bold text-accent">{money(outstanding)}</div></div><div className="panel rounded-xl p-5"><div className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Open records</div><div className="mt-2 font-mono text-2xl font-bold">{q.data?.filter(l => l.balanceRemaining > 0).length || 0}</div></div></div><div className="panel overflow-x-auto rounded-xl p-5">{q.isLoading ? <Loading /> : q.isError ? <Failed onRetry={() => q.refetch()} /> : q.data?.length ? <table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3">Person</th><th className="pb-3">Direction</th><th className="pb-3">Given</th><th className="pb-3">Remaining</th><th className="pb-3">Date</th></tr></thead><tbody className="divide-y divide-border/70">{q.data.map(l => <tr key={l.id} data-testid={`row-loan-${l.id}`}><td className="py-3 font-semibold">{l.personName}<div className="text-[11px] text-muted-foreground">{l.phone}</div></td><td className="py-3"><span className={`inline-flex items-center gap-1 text-xs font-bold ${l.loanType === 'lent' ? 'text-accent' : 'text-primary'}`}>{l.loanType === 'lent' ? <ArrowUpRight size={14} /> : <ArrowDownRight size={14} />}{l.loanType}</span></td><td className="py-3 font-mono">{money(l.amount)}</td><td className="py-3 font-mono font-bold">{money(l.balanceRemaining)}</td><td className="py-3 font-mono text-xs text-muted-foreground">{shortDate(l.dateGiven)}</td></tr>)}</tbody></table> : <Empty title="No loans on the books" detail="Keep borrowed and lent money visible, even when it feels small." action={<Button onClick={() => setModal(true)} testId="button-empty-add-loan"><Plus size={15} /> Add loan</Button>} />}</div>{modal && <Modal title="Add loan" eyebrow="Money trail" onClose={() => setModal(false)}><form onSubmit={submit} className="grid gap-3"><Field label="Person / business" name="loan-person" value={form.personName} onChange={v => setForm({ ...form, personName: v })} required /><div className="grid grid-cols-2 gap-3"><Field label="Phone" name="loan-phone" value={form.phone} onChange={v => setForm({ ...form, phone: v })} /><label className="grid gap-1.5 text-xs font-semibold text-muted-foreground">Direction<select data-testid="select-loan-type" value={form.loanType} onChange={e => setForm({ ...form, loanType: e.target.value })} className="h-10 rounded-lg border border-input bg-background px-3 text-sm"><option value="lent">I lent</option><option value="borrowed">I borrowed</option></select></label></div><Field label="Amount" name="loan-amount" type="number" value={form.amount} onChange={v => setForm({ ...form, amount: v })} required /><Field label="Description" name="loan-description" value={form.description} onChange={v => setForm({ ...form, description: v })} /><Button type="submit" disabled={create.isPending} testId="button-save-loan">{create.isPending ? 'Saving…' : 'Save loan'}</Button></form></Modal>}</div>;
}

function Reports() {
  const dash = useGetDashboard({ query: { queryKey: getGetDashboardQueryKey() } }); const sales = useGetSales({ query: { queryKey: getGetSalesQueryKey() } }); const expenses = useGetExpenses({ query: { queryKey: getGetExpensesQueryKey() } });
  const saleTotal = sales.data?.reduce((a, s) => a + s.totalAmount, 0) || dash.data?.dailySales || 0; const collected = sales.data?.reduce((a, s) => a + s.paidAmount, 0) || 0; const receivable = sales.data?.reduce((a, s) => a + s.dueAmount, 0) || dash.data?.buyerDebt || 0; const costs = expenses.data?.reduce((a, e) => a + e.amount, 0) || 0;
  return <div className="animate-in"><PageIntro eyebrow="Read the pattern" title="Reports" detail="A short view of profitability and what is still waiting to be collected." action={<Button variant="outline" onClick={() => window.print()} testId="button-print-report"><Printer size={16} /> Print report</Button>} /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Gross sales" value={money(saleTotal)} note="From loaded invoices" icon={BarChart3} /><Stat label="Collected" value={money(collected)} note="Cash and transfers" icon={Banknote} tone="warning" /><Stat label="Receivables" value={money(receivable)} note="Still to collect" icon={CreditCard} tone="accent" /><Stat label="Expenses" value={money(costs)} note="Logged operating costs" icon={Receipt} tone="blue" /></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><section className="panel rounded-xl p-6"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-primary text-primary-foreground"><BarChart3 size={18} /></div><div><h3 className="font-bold">Profitability snapshot</h3><p className="text-xs text-muted-foreground">What the day is really returning</p></div></div><div className="mt-7 grid gap-5"><div><div className="mb-2 flex justify-between text-sm font-semibold"><span>Gross sales</span><span className="font-mono">{money(saleTotal)}</span></div><div className="h-2 rounded-full bg-muted"><div className="h-2 w-full rounded-full bg-primary" /></div></div><div><div className="mb-2 flex justify-between text-sm font-semibold"><span>Profit after expenses</span><span className="font-mono text-emerald-700">{money((dash.data?.todaysProfit || saleTotal - costs))}</span></div><div className="h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-secondary" style={{ width: `${Math.min(Math.max(((dash.data?.todaysProfit || saleTotal - costs) / Math.max(saleTotal, 1)) * 100, 4), 100)}%` }} /></div></div><div className="grid grid-cols-2 gap-3 pt-2"><div className="rounded-lg bg-muted p-3"><div className="text-[10px] font-bold uppercase text-muted-foreground">Margin</div><div className="mt-1 font-mono text-lg font-bold">{saleTotal ? `${Math.round(((dash.data?.todaysProfit || saleTotal - costs) / saleTotal) * 100)}%` : '—'}</div></div><div className="rounded-lg bg-muted p-3"><div className="text-[10px] font-bold uppercase text-muted-foreground">Cash conversion</div><div className="mt-1 font-mono text-lg font-bold">{saleTotal ? `${Math.round((collected / saleTotal) * 100)}%` : '—'}</div></div></div></div></section><section className="panel rounded-xl p-6"><h3 className="font-bold">Receivables focus</h3><p className="mt-1 text-xs text-muted-foreground">Cash that can still come home</p><div className="mt-8 text-center"><div className="mx-auto grid h-28 w-28 place-items-center rounded-full border-[12px] border-secondary/60 border-r-primary"><div className="font-mono text-xl font-bold">{money(receivable)}</div></div><div className="mt-5 text-sm font-semibold">Outstanding buyer balance</div><Link href="/buyers" data-testid="link-reports-buyers" className="mt-4 inline-flex items-center gap-1 text-xs font-bold text-primary hover:underline">Open buyer book <ChevronRight size={14} /></Link></div></section></div></div>;
}

function NotFound() { return <div className="grid min-h-[70vh] place-items-center"><div className="text-center"><div className="font-mono text-sm text-accent">404 / off the route</div><h1 className="mt-2 text-4xl font-bold">That page isn't on the floor plan.</h1><Link href="/" data-testid="link-back-home" className="mt-5 inline-flex rounded-lg bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">Back to overview</Link></div></div>; }
function Router() { return <ErrorBoundary resetKey={location.pathname}><Switch><Route path="/" component={Dashboard} /><Route path="/sales" component={Sales} /><Route path="/buyers" component={Buyers} /><Route path="/products" component={Products} /><Route path="/suppliers" component={Suppliers} /><Route path="/expenses" component={Expenses} /><Route path="/loans" component={Loans} /><Route path="/reports" component={Reports} /><Route component={NotFound} /></Switch></ErrorBoundary>; }
function App() { return <QueryClientProvider client={queryClient}><TooltipProvider><Shell><Router /></Shell><Toaster /></TooltipProvider></QueryClientProvider>; }
export default App;